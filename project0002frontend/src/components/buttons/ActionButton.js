import React from 'react'

const ActionButton = ({}) => {
    return (
        <div>
            button
        </div>
    )
}

ActionButton.defaultProps={
    isDraggable: false,
}

export default ActionButton
